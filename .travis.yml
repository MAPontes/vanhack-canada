matrix:
  include:
    - language: ruby
      stage: test
      install: gem install rspec
      before_script:
        - cd app/test
      script:
        - bundle exec rspec

    - language: C
      stage: test
      before_script:
        - cd infra/
      script:
        - make

    - language: C
      stage: deploy
      script:
        - make
